- name: Setup System Session Logging
  tags: always
  zpe.nodegrid.system:
    system_logging:
      enable_session_logging: "{{ system_logging_enable_session_logging }}"
      enable_session_logging_alerts: "{{ system_logging_enable_session_logging_alerts }}"
