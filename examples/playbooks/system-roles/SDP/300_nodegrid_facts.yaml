- hosts: all
  gather_facts: true
  gather_timeout: 30
  collections:
    - ansible.builtin
    - zpe.nodegrid

  tasks:
    - name: Get Facts from Nodegrid
      zpe.nodegrid.nodegrid_facts:

    - name: Ansible and Nodegrid Facts
      debug:
        var: ansible_facts
